import { defineConfig } from "vite";
import vue from "@vitejs/plugin-vue";

// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  base: "./",
  server: {
    port: 5173,
    strictPort: true
  },
  build: {
    outDir: "../Sources/{% swift_module_name %}/Resources/assets",
    emptyOutDir: true
  }
});
